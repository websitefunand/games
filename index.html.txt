<!DOCTYPE html>
<html lang="en"><head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>fun and games</title>
<style>
    * { box-sizing: border-box; }

    :root {
      --bg-1: #0b1220;
      --bg-2: #111827;
      --panel: rgba(15, 23, 42, 0.72);
      --panel-strong: rgba(15, 23, 42, 0.92);
      --card-border: rgba(255, 255, 255, 0.12);
      --text: #f8fafc;
      --muted: #a5b4cc;
      --shadow: 0 20px 60px rgba(0, 0, 0, 0.34);
      --radius-xl: 34px;
      --radius-lg: 24px;
      --radius-md: 18px;
      --duck: linear-gradient(135deg, #bbf451 0%, #7cc61c 100%);
      --wordle: linear-gradient(135deg, #87c87a 0%, #4a9a57 100%);
      --minecraft: linear-gradient(135deg, #65b73b 0%, #285d1f 100%);
      --plinko: linear-gradient(135deg, #191919 0%, #050505 100%);
      --snow: linear-gradient(135deg, #b98946 0%, #7a5225 100%);
      --white-card: linear-gradient(135deg, #ffffff 0%, #edf2f7 100%);
      --fnaf: linear-gradient(135deg, #0d0d0d 0%, #1f1f1f 100%);
      --ragdoll: linear-gradient(135deg, #f7dd87 0%, #e7b949 100%);
      --doodle: linear-gradient(135deg, #fff8d7 0%, #f2e69c 100%);
      --geometry: linear-gradient(135deg, #55ebff 0%, #0ba7cf 100%);
      --accent: #60a5fa;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      color: var(--text);
      background:
        radial-gradient(circle at top left, rgba(96, 165, 250, 0.18), transparent 30%),
        radial-gradient(circle at top right, rgba(168, 85, 247, 0.16), transparent 28%),
        linear-gradient(160deg, var(--bg-1) 0%, var(--bg-2) 52%, #09111f 100%);
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      background-image:
        linear-gradient(rgba(255,255,255,0.035) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.035) 1px, transparent 1px);
      background-size: 32px 32px;
      mask-image: linear-gradient(to bottom, rgba(0,0,0,0.65), transparent 92%);
    }

    .page-wrap {
      width: min(1400px, calc(100% - 32px));
      margin: 0 auto;
      padding: 28px 0 42px;
    }

    .shell {
      background: var(--panel);
      border: 1px solid var(--card-border);
      border-radius: 38px;
      box-shadow: var(--shadow);
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);
      overflow: hidden;
    }

    .hero {
      position: relative;
      padding: 28px 28px 12px;
      border-bottom: 1px solid rgba(255,255,255,0.08);
      background:
        radial-gradient(circle at 20% 0%, rgba(96, 165, 250, 0.14), transparent 30%),
        radial-gradient(circle at 80% 15%, rgba(56, 189, 248, 0.12), transparent 24%),
        linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0));
    }

    .topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 26px;
      flex-wrap: wrap;
    }

    .chip-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      min-height: 54px;
      padding: 0 22px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(255,255,255,0.08);
      color: var(--text);
      text-decoration: none;
      font-weight: 700;
      letter-spacing: -0.02em;
      transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.05);
    }

    .chip-btn:hover {
      transform: translateY(-2px);
      background: rgba(255,255,255,0.12);
      border-color: rgba(255,255,255,0.25);
    }

    .chip-btn.chatbox {
      background: linear-gradient(135deg, #38bdf8, #3b82f6);
      border-color: rgba(255,255,255,0.1);
      color: white;
    }

    .hero-main {
      display: grid;
      grid-template-columns: 1.15fr 0.85fr;
      gap: 24px;
      align-items: end;
      padding-bottom: 20px;
    }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 12px;
      border-radius: 999px;
      font-size: 0.82rem;
      font-weight: 700;
      color: #dbeafe;
      background: rgba(59, 130, 246, 0.18);
      border: 1px solid rgba(96, 165, 250, 0.25);
      margin-bottom: 16px;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    .site-title {
      margin: 0;
      font-size: clamp(3rem, 6vw, 5.6rem);
      line-height: 0.9;
      letter-spacing: -0.07em;
      font-weight: 900;
    }

    .site-subtitle {
      margin: 16px 0 0;
      max-width: 700px;
      font-size: 1.05rem;
      line-height: 1.65;
      color: var(--muted);
    }

    .hero-stats {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 14px;
    }

    .stat-card {
      padding: 18px;
      border-radius: 22px;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.08);
      min-height: 110px;
    }

    .stat-label {
      font-size: 0.84rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--muted);
      margin-bottom: 8px;
      font-weight: 700;
    }

    .stat-value {
      font-size: 2rem;
      font-weight: 800;
      letter-spacing: -0.05em;
      margin-bottom: 4px;
    }

    .stat-note {
      font-size: 0.95rem;
      color: #d8e5f7;
    }

    .content {
      padding: 28px;
    }

    .section-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 22px;
      flex-wrap: wrap;
    }

    .section-title {
      margin: 0;
      font-size: 1.5rem;
      letter-spacing: -0.04em;
    }

    .section-copy {
      margin: 6px 0 0;
      color: var(--muted);
      font-size: 0.98rem;
    }

    .page-badge {
      padding: 10px 14px;
      border-radius: 999px;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.08);
      color: #d8e5f7;
      font-weight: 700;
    }

    .pages {
      position: relative;
      min-height: 640px;
    }

    .games-page { display: none; }
    .games-page.active { display: block; }

    .games-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 22px;
    }

    .card {
      position: relative;
      min-height: 215px;
      padding: 18px;
      border-radius: var(--radius-xl);
      border: 1px solid rgba(255,255,255,0.12);
      text-decoration: none;
      color: inherit;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      box-shadow: 0 18px 40px rgba(0,0,0,0.16);
      transition: transform 0.22s ease, box-shadow 0.22s ease, border-color 0.22s ease;
      isolation: isolate;
    }

    .card::before {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(180deg, rgba(255,255,255,0.18), rgba(255,255,255,0.02) 46%, rgba(0,0,0,0.06));
      opacity: 0.8;
      z-index: -1;
    }

    .card::after {
      content: "↗";
      position: absolute;
      top: 18px;
      right: 18px;
      width: 36px;
      height: 36px;
      display: grid;
      place-items: center;
      border-radius: 50%;
      font-size: 1rem;
      font-weight: 900;
      background: rgba(255,255,255,0.22);
      color: rgba(0,0,0,0.76);
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 26px 50px rgba(0,0,0,0.28);
      border-color: rgba(255,255,255,0.24);
    }

    .card-top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 16px;
    }

    .thumb {
      width: 108px;
      height: 108px;
      border-radius: 28px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(255,255,255,0.18);
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.2);
      overflow: hidden;
      flex-shrink: 0;
    }

    .thumb img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      display: block;
      user-select: none;
      -webkit-user-drag: none;
    }

    .game-tag {
      align-self: flex-start;
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(255,255,255,0.2);
      font-size: 0.8rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    .card-bottom {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .label {
      font-size: 1.65rem;
      font-weight: 900;
      letter-spacing: -0.05em;
      line-height: 1;
      text-transform: lowercase;
    }

    .desc {
      font-size: 0.98rem;
      line-height: 1.45;
      color: rgba(0,0,0,0.72);
      max-width: 26ch;
      font-weight: 600;
    }

    .light-text .label,
    .light-text .desc,
    .light-text .game-tag { color: #f8fafc; }

    .light-text .desc { color: rgba(248,250,252,0.82); }
    .light-text::after { color: #fff; }

    .duckmath { background: var(--duck); color: #102407; }
    .wordle { background: var(--wordle); color: #f6fff7; }
    .minecraft { background: var(--minecraft); color: #efffed; }
    .plinko { background: var(--plinko); color: #f8fafc; }
    .snow { background: var(--snow); color: #fffaf1; }
    .renweb, .chatgpt, .neal, .poki, .soundboard { background: var(--white-card); color: #0f172a; }
    .fnaf { background: var(--fnaf); color: #f8fafc; }
    .ragdoll { background: var(--ragdoll); color: #3a2b04; }
    .doodle { background: var(--doodle); color: #315d15; }
    .geometry { background: var(--geometry); color: #03131d; }

    .wordle .thumb,
    .doodle .thumb {
      font-size: 3.5rem;
      font-weight: 900;
      letter-spacing: -0.08em;
    }

    .plinko .thumb-text {
      font-family: "Brush Script MT", "Segoe Script", cursive;
      font-size: 3.5rem;
      color: #fff;
      line-height: 1;
      transform: rotate(-4deg);
      white-space: nowrap;
    }

    .minecraft .thumb img,
    .fnaf .thumb img,
    .subway .thumb img,
    .ragdoll .thumb img { object-fit: cover; }

    .geometry .thumb {
      gap: 8px;
      flex-wrap: wrap;
      align-content: center;
      justify-content: center;
    }

    .geometry .thumb span {
      width: 20px;
      height: 20px;
      background: #fff;
      display: block;
      transform: rotate(45deg);
      border: 3px solid #000;
    }

    .page-nav {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 14px;
      margin-top: 26px;
    }

    .nav-arrow {
      width: 54px;
      height: 54px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(255,255,255,0.06);
      color: var(--text);
      font-size: 1.45rem;
      cursor: pointer;
      transition: transform 0.18s ease, background 0.18s ease;
    }

    .nav-arrow:hover {
      transform: translateY(-2px);
      background: rgba(255,255,255,0.12);
    }

    .page-indicator {
      min-width: 130px;
      text-align: center;
      padding: 14px 18px;
      border-radius: 999px;
      font-size: 1rem;
      font-weight: 800;
      letter-spacing: 0.02em;
      background: rgba(255,255,255,0.08);
      border: 1px solid rgba(255,255,255,0.12);
      color: #e5eefc;
    }



    .safety-note {
      margin-bottom: 22px;
      padding: 16px 18px;
      border-radius: 22px;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      color: #dbeafe;
      line-height: 1.6;
      font-weight: 600;
    }

    .source-note {
      margin-top: 8px;
      font-size: 0.82rem;
      line-height: 1.35;
      color: rgba(0,0,0,0.6);
      font-weight: 700;
    }

    .light-text .source-note { color: rgba(248,250,252,0.78); }
    .announcement-bar {
      display: none;
      margin-bottom: 22px;
      padding: 16px 18px;
      border-radius: 22px;
      background: linear-gradient(135deg, rgba(59,130,246,0.18), rgba(14,165,233,0.14));
      border: 1px solid rgba(96,165,250,0.22);
      color: #eaf3ff;
      font-weight: 700;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.05);
    }

    .modal-overlay {
      position: fixed;
      inset: 0;
      display: none;
      align-items: center;
      justify-content: center;
      padding: 24px;
      background: rgba(2, 6, 23, 0.7);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      z-index: 9999;
    }

    .modal-overlay.open {
      display: flex;
    }

    .modal-card {
      width: min(760px, 100%);
      max-height: min(80vh, 900px);
      overflow: auto;
      padding: 24px;
      border-radius: 28px;
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(15, 23, 42, 0.96);
      box-shadow: 0 28px 80px rgba(0, 0, 0, 0.42);
    }

    .modal-top {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 16px;
      flex-wrap: wrap;
    }

    .modal-title {
      margin: 0;
      font-size: 1.8rem;
      font-weight: 900;
      letter-spacing: -0.05em;
    }

    .modal-copy {
      margin: 0 0 16px;
      color: var(--muted);
      line-height: 1.6;
    }

    .patch-notes-body {
      white-space: pre-wrap;
      line-height: 1.7;
      color: #eef5ff;
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 22px;
      padding: 18px;
    }

    @media (max-width: 1100px) {
      .hero-main,
      .games-grid {
        grid-template-columns: 1fr 1fr;
      }

      .hero-main {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 760px) {
      .page-wrap {
        width: min(100% - 20px, 1400px);
        padding: 12px 0 24px;
      }

      .hero,
      .content {
        padding: 18px;
      }

      .topbar {
        flex-direction: column;
        align-items: stretch;
      }

      .chip-btn {
        width: 100%;
      }

      .hero-stats,
      .games-grid {
        grid-template-columns: 1fr;
      }

      .section-head {
        align-items: flex-start;
      }

      .card {
        min-height: 190px;
      }

      .thumb {
        width: 92px;
        height: 92px;
        border-radius: 24px;
      }

      .label {
        font-size: 1.45rem;
      }

      .desc {
        font-size: 0.92rem;
      }
    }
  </style>
</head>
<body>
  <div class="page-wrap">
    <div class="shell">
      <header class="hero">
        <div class="topbar">
          <div style="display:flex; gap:16px; flex-wrap:wrap;">
            <a class="chip-btn" href="https://docs.google.com/forms/d/e/1FAIpQLSe89iu2REXajdvhOfnjQLso4zF0EjAXQAud3Ha9CIE8BrQtoQ/viewform?usp=dialog">request a game</a>
            <a class="chip-btn chatbox" href="chatbox.html">open chatbox</a>
            <button class="chip-btn" id="patchNotesBtn" type="button">patch notes</button>
          </div>
        </div>

        <div class="hero-main">
          <div>
            <div class="eyebrow">GAME HUB</div>
            <h1 class="site-title">fun and games</h1>
            <p class="site-subtitle" id="siteSubtitle">a game website to just generally have fun!</p>
          </div>

          <div class="hero-stats">
            <div class="stat-card">
              <div class="stat-label">Games</div>
              <div class="stat-value" id="gamesCount">0</div>
              <div class="stat-note">live from firebase</div>
            </div>
            <div class="stat-card">
              <div class="stat-label">pages</div>
              <div class="stat-value" id="pagesCount">0</div>
              <div class="stat-note">auto-built by your game list</div>
            </div>
          </div>
        </div>
      </header>

      <main class="content">
        <div class="announcement-bar" id="announcementBar"></div>
        <div class="safety-note">External games/apps are clearly labeled before launch. This site may open or embed content from third-party providers and does not ask for passwords or payment information.</div>

        <div class="section-head">
          <div>
            <h2 class="section-title" id="libraryTitle">library</h2>
            <p class="section-copy" id="libraryCopy">Pick a game or tool below to launch it instantly.</p>
          </div>
          <div class="page-badge" id="pageBadge">currently viewing page 1</div>
        </div>

        <div class="pages" id="pagesContainer"></div>

        <div class="page-nav">
          <button class="nav-arrow" id="prevBtn" aria-label="Previous page">←</button>
          <div class="page-indicator" id="pageIndicator">page 1 of 1</div>
          <button class="nav-arrow" id="nextBtn" aria-label="Next page">→</button>
        </div>
      </main>
    </div>
  </div>

  <div class="modal-overlay" id="patchModal" aria-hidden="true">
    <div class="modal-card">
      <div class="modal-top">
        <h2 class="modal-title">patch notes</h2>
        <button class="chip-btn" id="closePatchNotesBtn" type="button">close</button>
      </div>
      <p class="modal-copy" id="patchNotesUpdated">latest homepage updates and changes.</p>
      <div class="patch-notes-body" id="patchNotesContent">No patch notes yet.</div>
    </div>
  </div>

  <div class="modal-overlay" id="adminLoginModal" aria-hidden="true">
    <div class="modal-card" style="width:min(460px,100%);">
      <div class="modal-top">
        <h2 class="modal-title">admin login</h2>
        <button class="chip-btn" id="closeAdminLoginBtn" type="button">close</button>
      </div>
      <p class="modal-copy">shift + click patch notes to open this.</p>
      <div style="display:grid; gap:14px;">
        <input id="adminUsername" type="text" placeholder="username" style="width:100%; border:1px solid rgba(255,255,255,0.1); background:rgba(8,15,28,0.65); color:var(--text); border-radius:18px; padding:14px 16px; font:inherit; outline:none;">
        <input id="adminPassword" type="password" placeholder="password" style="width:100%; border:1px solid rgba(255,255,255,0.1); background:rgba(8,15,28,0.65); color:var(--text); border-radius:18px; padding:14px 16px; font:inherit; outline:none;">
        <div style="display:flex; gap:12px; flex-wrap:wrap;">
          <button class="chip-btn chatbox" id="adminLoginBtn" type="button">login</button>
          <div id="adminLoginStatus" style="align-self:center; color:var(--muted);"></div>
        </div>
      </div>
    </div>
  </div>

  <script type="module">
    import { initializeApp } from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-app.js';
    import { getDatabase, ref, get, set, onValue } from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-database.js';

    const firebaseConfig = {
      apiKey: 'AIzaSyDvKpZacWOgca6PtoA6wP2QFX3JbI-Dvw4',
      authDomain: 'funandgames-4e8c1.firebaseapp.com',
      databaseURL: 'https://funandgames-4e8c1-default-rtdb.firebaseio.com',
      projectId: 'funandgames-4e8c1',
      storageBucket: 'funandgames-4e8c1.firebasestorage.app',
      messagingSenderId: '240713956788',
      appId: '1:240713956788:web:37b64a7a534f1e2bea06e1',
      measurementId: 'G-KLSV3Q73MR'
    };

    const DEFAULTS = {
      heroSubtitle: 'a game website to just generally have fun!',
      libraryTitle: 'library',
      libraryCopy: 'Pick a game or tool below to launch it instantly.',
      announcementEnabled: false,
      announcementText: '',
      siteMode: 'normal',
      patchNotes: 'No patch notes yet.',
      patchNotesUpdated: 'latest homepage updates and changes.',
      jokeOfTheDay: 'No joke set yet.',
      jokeUpdated: 'fresh from the admin panel.'
    };

    const DEFAULT_GAMES = [
  {
    "id": "duckmath",
    "title": "duckmath",
    "description": "a bunch of different games in one website.",
    "embedUrl": "https://resources.a.staticdata.top/",
    "sourceUrl": "https://resources.a.staticdata.top/",
    "thumbnailUrl": "https://duck-math.freetls.fastly.net/imgs/duck.webp",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "math",
    "page": 1,
    "type": "game",
    "themeClass": "duckmath",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 1
  },
  {
    "id": "wordle",
    "title": "wordle",
    "description": "fun word guessing and its unlimited",
    "embedUrl": "wordle.html",
    "sourceUrl": "wordle.html",
    "thumbnailUrl": "",
    "thumbText": "W",
    "thumbHtml": "W",
    "tag": "word",
    "page": 1,
    "type": "game",
    "themeClass": "wordle light-text",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 2
  },
  {
    "id": "minecraft",
    "title": "minecraft",
    "description": "a game of survival or sandbox",
    "embedUrl": "https://classroomlesson.github.io/basic-ruffle-player/html/minecraft/duckcraft/duckcraft-v1.html",
    "sourceUrl": "https://classroomlesson.github.io/basic-ruffle-player/html/minecraft/duckcraft/duckcraft-v1.html",
    "thumbnailUrl": "https://i.imgur.com/nKsYRdJ.png",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "sandbox",
    "page": 1,
    "type": "game",
    "themeClass": "minecraft light-text",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 3
  },
  {
    "id": "plinko",
    "title": "plinko",
    "description": "a gambling game. try not to lose money!",
    "embedUrl": "https://plinko-game-online.github.io/",
    "sourceUrl": "https://plinko-game-online.github.io/",
    "thumbnailUrl": "",
    "thumbText": "Plinko",
    "thumbHtml": "<div class=\"thumb-text\">Plinko</div>",
    "tag": "arcade",
    "page": 1,
    "type": "game",
    "themeClass": "plinko light-text",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 4
  },
  {
    "id": "snow-rider",
    "title": "snow rider",
    "description": "its fun racing in the snow!",
    "embedUrl": "https://itsvijaysingh.github.io/Snow-Rider3D/",
    "sourceUrl": "https://itsvijaysingh.github.io/Snow-Rider3D/",
    "thumbnailUrl": "https://merlot.org/merlot/getMaterialImage.htm?id=773417944",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "racing",
    "page": 1,
    "type": "game",
    "themeClass": "snow light-text",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 5
  },
  {
    "id": "renweb",
    "title": "renweb",
    "description": "check your grades! i bet they're good!",
    "embedUrl": "https://familyportal.renweb.com",
    "sourceUrl": "https://familyportal.renweb.com",
    "thumbnailUrl": "https://p.kindpng.com/picc/s/594-5943254_facts-facts-renweb-hd-png-download.png",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "school",
    "page": 1,
    "type": "app",
    "themeClass": "renweb",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "redirect",
    "hidden": false,
    "locked": false,
    "order": 6
  },
  {
    "id": "fnaf-1",
    "title": "fnaf 1",
    "description": "a point and click classic game of strategic door closing",
    "embedUrl": "https://algebraschoolstudy.github.io/SchoolStudyGuidesList/1/index.html",
    "sourceUrl": "https://algebraschoolstudy.github.io/SchoolStudyGuidesList/1/index.html",
    "thumbnailUrl": "https://image.api.playstation.com/gs2-sec/appkgo/prod/CUSA18362_00/1/i_cd6a177c374f229f9cd8e1164a78d49f78af97820747d480daf8cdd3e965c248/i/icon0.png",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "horror",
    "page": 1,
    "type": "game",
    "themeClass": "fnaf light-text",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 7
  },
  {
    "id": "ai-chatbot",
    "title": "ai chatbot",
    "description": "get help on your daily life with this ai",
    "embedUrl": "aichatbot.html",
    "sourceUrl": "aichatbot.html",
    "thumbnailUrl": "https://upload.wikimedia.org/wikipedia/commons/0/04/ChatGPT_logo.svg",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "ai",
    "page": 1,
    "type": "app",
    "themeClass": "chatgpt",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 8
  },
  {
    "id": "neal-fun",
    "title": "neal.fun",
    "description": "a unique website containing tons of excitng games like infinite craft",
    "embedUrl": "https://neal.fun",
    "sourceUrl": "https://neal.fun",
    "thumbnailUrl": "https://cdn.buymeacoffee.com/uploads/profile_pictures/2025/10/IG9EK8jZj0v2CEW4.png@300w_0e.webp",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "fun",
    "page": 1,
    "type": "app",
    "themeClass": "neal",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "redirect",
    "hidden": false,
    "locked": false,
    "order": 9
  },
  {
    "id": "poki",
    "title": "poki",
    "description": "a website with a whole bunch of games",
    "embedUrl": "construction.html",
    "sourceUrl": "construction.html",
    "thumbnailUrl": "https://play-lh.googleusercontent.com/FBwBJqAv2KjTXRDRaEkrY3r2PJUWZbru1Y-6VyQY4Sku776mLJ2fIh1nMqzsmGdXUPI=w240-h480-rw",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "hub",
    "page": 2,
    "type": "game",
    "themeClass": "poki",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 10
  },
  {
    "id": "soundboard",
    "title": "soundboard",
    "description": "funny sound effects, dont get in trouble!",
    "embedUrl": "https://soundinstants.com",
    "sourceUrl": "https://soundinstants.com",
    "thumbnailUrl": "https://upload.wikimedia.org/wikipedia/commons/2/21/Speaker_Icon.svg",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "audio",
    "page": 2,
    "type": "app",
    "themeClass": "soundboard",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "redirect",
    "hidden": false,
    "locked": false,
    "order": 11
  },
  {
    "id": "subway-surfers",
    "title": "subway surfers",
    "description": "run from the police, and dont get ran over by a train!",
    "embedUrl": "http://subwaysurfers76.github.io",
    "sourceUrl": "http://subwaysurfers76.github.io",
    "thumbnailUrl": "https://img.poki-cdn.com/cdn-cgi/image/q=78,scq=50,width=1200,height=1200,fit=cover,f=png/7f4c7dc492227ec127a47e4b3197754e/subway-surfers.png",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "runner",
    "page": 2,
    "type": "game",
    "themeClass": "subway light-text",
    "cardStyle": "background: linear-gradient(135deg, #ff7d36 0%, #ffbb38 100%); color:#2f1700;",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 12
  },
  {
    "id": "ragdoll-hit",
    "title": "ragdoll hit",
    "description": "a graphic game of hitting people!",
    "embedUrl": "ragdoll_redesign.html",
    "sourceUrl": "ragdoll_redesign.html",
    "thumbnailUrl": "https://img.poki-cdn.com/cdn-cgi/image/q=78,scq=50,width=1200,height=1200,fit=cover,f=png/3c892779be9dabb3589586c83ad3eeed/ragdoll-hit.png",
    "thumbText": "",
    "thumbHtml": "",
    "tag": "fight",
    "page": 2,
    "type": "game",
    "themeClass": "ragdoll",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 13
  },
  {
    "id": "doodle-jump",
    "title": "doodle jump",
    "description": "jumping around to get the highest score possible",
    "embedUrl": "doodle.html",
    "sourceUrl": "doodle.html",
    "thumbnailUrl": "",
    "thumbText": "D",
    "thumbHtml": "D",
    "tag": "classic",
    "page": 2,
    "type": "game",
    "themeClass": "doodle",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 14
  },
  {
    "id": "geometry-dash",
    "title": "geometry dash",
    "description": "try not to get spiked, but have fun!",
    "embedUrl": "dash.html",
    "sourceUrl": "dash.html",
    "thumbnailUrl": "",
    "thumbText": "",
    "thumbHtml": "<span></span><span></span><span></span><span></span>",
    "tag": "platform",
    "page": 2,
    "type": "game",
    "themeClass": "geometry",
    "cardStyle": "",
    "platform": "browser",
    "statusText": "live",
    "addedText": "classic",
    "credits": "original creators",
    "sourceLabel": "launch url",
    "openMode": "embed",
    "hidden": false,
    "locked": false,
    "order": 15
  }
];
    const LOGIN_COMBOS = [
      { username: 'crustycruzzy', password: 'Cruz10312' },
      { username: 'admin', password: 'backup' }
    ];

    const app = initializeApp(firebaseConfig);
    const db = getDatabase(app);
    const settingsRef = ref(db, 'homepageAdmin/settings');
    const gamesRef = ref(db, 'homepageAdmin/games');

    const el = {
      siteSubtitle: document.getElementById('siteSubtitle'),
      libraryTitle: document.getElementById('libraryTitle'),
      libraryCopy: document.getElementById('libraryCopy'),
      announcementBar: document.getElementById('announcementBar'),
      pagesContainer: document.getElementById('pagesContainer'),
      prevBtn: document.getElementById('prevBtn'),
      nextBtn: document.getElementById('nextBtn'),
      pageIndicator: document.getElementById('pageIndicator'),
      pageBadge: document.getElementById('pageBadge'),
      gamesCount: document.getElementById('gamesCount'),
      pagesCount: document.getElementById('pagesCount'),
      patchNotesBtn: document.getElementById('patchNotesBtn'),
      patchModal: document.getElementById('patchModal'),
      closePatchNotesBtn: document.getElementById('closePatchNotesBtn'),
      patchNotesContent: document.getElementById('patchNotesContent'),
      patchNotesUpdated: document.getElementById('patchNotesUpdated'),
      adminLoginModal: document.getElementById('adminLoginModal'),
      closeAdminLoginBtn: document.getElementById('closeAdminLoginBtn'),
      adminUsername: document.getElementById('adminUsername'),
      adminPassword: document.getElementById('adminPassword'),
      adminLoginBtn: document.getElementById('adminLoginBtn'),
      adminLoginStatus: document.getElementById('adminLoginStatus')
    };

    let currentPage = 0;
    let renderedPages = [];

    function escapeHtml(value='') {
      return String(value).replace(/[&<>"']/g, (ch) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
    }

    function buildThumb(game) {
      if (game.thumbnailUrl) return `<img src="${escapeHtml(game.thumbnailUrl)}" alt="${escapeHtml(game.title)}">`;
      if (game.thumbHtml) return game.thumbHtml;
      return escapeHtml(game.thumbText || game.title?.[0] || '?');
    }


    function isExternalUrl(value='') {
      return /^https?:\/\//i.test(String(value || '').trim());
    }

    function getHostLabel(value='') {
      try {
        const url = new URL(value, window.location.href);
        return url.host || 'external website';
      } catch {
        return 'external website';
      }
    }

    function buildTextStyle(game) {
      if (!game || !game.textColor) return '';
      const value = escapeHtml(game.textColor);
      return ` style="color:${value};"`;
    }

    function buildCard(game) {
      const launchUrl = game.sourceUrl || game.embedUrl || '#';
      const href = game.locked
        ? 'construction.html'
        : (game.openMode === 'redirect'
            ? launchUrl
            : `game.html?id=${encodeURIComponent(game.id)}`);
      const classes = ['card'];
      if (game.themeClass) classes.push(...String(game.themeClass).split(/\s+/).filter(Boolean));
      if (!game.themeClass) classes.push('custom-card');
      const style = game.cardStyle ? ` style="${escapeHtml(game.cardStyle)}"` : '';
      const external = isExternalUrl(launchUrl);
      const hostNote = external ? `<div class="source-note">hosted by ${escapeHtml(getHostLabel(launchUrl))}</div>` : `<div class="source-note">opens on this site</div>`;
      return `
        <a class="${classes.join(' ')}" href="${escapeHtml(href)}" data-game-id="${escapeHtml(game.id)}" data-launch-url="${escapeHtml(launchUrl)}" data-open-mode="${escapeHtml(game.openMode || 'embed')}" data-external="${external ? 'true' : 'false'}" aria-label="${escapeHtml(game.title)}"${game.openMode === 'redirect' && !game.locked ? ' rel="noopener noreferrer nofollow"' : ''}${style}>
          <div class="card-top">
            <div class="thumb">${buildThumb(game)}</div>
            <div class="game-tag">${escapeHtml(game.tag || game.type || 'game')}</div>
          </div>
          <div class="card-bottom">
            <div class="label"${buildTextStyle(game)}>${escapeHtml(game.title)}</div>
            <div class="desc"${buildTextStyle(game)}>${escapeHtml(game.description || '')}</div>
            ${hostNote}
          </div>
        </a>`;
    }

    function renderPages(gamesObj) {
      const games = Object.values(gamesObj || {})
        .filter((game) => game && !game.hidden)
        .sort((a, b) => {
          const orderA = Number.isFinite(Number(a.order)) ? Number(a.order) : Number.MAX_SAFE_INTEGER;
          const orderB = Number.isFinite(Number(b.order)) ? Number(b.order) : Number.MAX_SAFE_INTEGER;
          return orderA - orderB || String(a.title || '').localeCompare(String(b.title || ''));
        });

      const cardsPerPage = 9;
      renderedPages = [];
      for (let i = 0; i < games.length; i += cardsPerPage) {
        renderedPages.push([i / cardsPerPage + 1, games.slice(i, i + cardsPerPage)]);
      }

      el.pagesContainer.innerHTML = renderedPages.map(([pageNumber, list], idx) => `
        <section class="games-page ${idx===0?'active':''}" data-page="${pageNumber}">
          <div class="games-grid">${list.map(buildCard).join('')}</div>
        </section>`).join('');

      el.gamesCount.textContent = String(games.length);
      el.pagesCount.textContent = String(renderedPages.length || 0);
      currentPage = Math.min(currentPage, Math.max(0, renderedPages.length - 1));
      updatePageUi();
    }

    function updatePageUi() {
      const sections = [...document.querySelectorAll('.games-page')];
      if (!sections.length) {
        el.pageIndicator.textContent = 'no games available';
        el.pageBadge.textContent = 'homepage empty';
        return;
      }
      sections.forEach((section, idx) => section.classList.toggle('active', idx === currentPage));
      el.pageIndicator.textContent = `page ${currentPage + 1} of ${sections.length}`;
      el.pageBadge.textContent = `currently viewing page ${currentPage + 1}`;
    }

    function applySettings(settings={}) {
      const merged = {...DEFAULTS, ...settings};
      el.siteSubtitle.textContent = merged.heroSubtitle;
      el.libraryTitle.textContent = merged.libraryTitle;
      el.libraryCopy.textContent = merged.libraryCopy;
      el.patchNotesContent.textContent = merged.patchNotes;
      el.patchNotesUpdated.textContent = merged.patchNotesUpdated;
      if (el.jokeText) el.jokeText.textContent = merged.jokeOfTheDay || DEFAULTS.jokeOfTheDay;
      if (el.jokeSubtitle) el.jokeSubtitle.textContent = merged.jokeUpdated || DEFAULTS.jokeUpdated;
      if (merged.announcementEnabled && merged.announcementText) {
        el.announcementBar.style.display = 'block';
        el.announcementBar.textContent = merged.announcementText;
      } else {
        el.announcementBar.style.display = 'none';
        el.announcementBar.textContent = '';
      }
    }

    async function seedDefaultsIfNeeded() {
      const [settingsSnap, gamesSnap] = await Promise.all([get(settingsRef), get(gamesRef)]);
      if (!settingsSnap.exists()) await set(settingsRef, DEFAULTS);
      if (!gamesSnap.exists()) {
        const payload = Object.fromEntries(DEFAULT_GAMES.map((game) => [game.id, game]));
        await set(gamesRef, payload);
      }
    }

    function openModal(modal) { modal.classList.add('open'); modal.setAttribute('aria-hidden','false'); }
    function closeModal(modal) { modal.classList.remove('open'); modal.setAttribute('aria-hidden','true'); }

    el.patchNotesBtn.addEventListener('click', () => {
      openModal(el.patchModal);
    });
    el.closePatchNotesBtn.addEventListener('click', ()=>closeModal(el.patchModal));
    el.closeAdminLoginBtn.addEventListener('click', ()=>closeModal(el.adminLoginModal));
    el.patchModal.addEventListener('click', (e)=>{ if(e.target===el.patchModal) closeModal(el.patchModal); });
    el.adminLoginModal.addEventListener('click', (e)=>{ if(e.target===el.adminLoginModal) closeModal(el.adminLoginModal); });
    if (el.adminLoginBtn) {
      el.adminLoginBtn.addEventListener('click', () => {
        el.adminLoginStatus.textContent = 'admin access is no longer available from the homepage.';
      });
    }


    let pendingExternalUrl = '';

    function closeExternalModal() {
      if (!el.externalModal) return;
      el.externalModal.classList.remove('open');
      el.externalModal.setAttribute('aria-hidden', 'true');
      pendingExternalUrl = '';
    }

    function openExternalModal(url='') {
      pendingExternalUrl = url;
      if (el.externalHostText) {
        el.externalHostText.textContent = `${getHostLabel(url)} — ${url}`;
      }
      if (el.externalModal) {
        el.externalModal.classList.add('open');
        el.externalModal.setAttribute('aria-hidden', 'false');
      }
    }

    if (el.closeExternalModalBtn) el.closeExternalModalBtn.addEventListener('click', closeExternalModal);
    if (el.cancelExternalBtn) el.cancelExternalBtn.addEventListener('click', closeExternalModal);
    if (el.continueExternalBtn) {
      el.continueExternalBtn.addEventListener('click', () => {
        if (!pendingExternalUrl) return;
        window.location.href = pendingExternalUrl;
      });
    }
    if (el.externalModal) {
      el.externalModal.addEventListener('click', (event) => {
        if (event.target === el.externalModal) closeExternalModal();
      });
    }

    el.pagesContainer.addEventListener('click', (event) => {
      const link = event.target.closest('a.card');
      if (!link) return;
      const external = link.dataset.external === 'true';
      const openMode = link.dataset.openMode || 'embed';
      const url = link.dataset.launchUrl || link.href;
      if (external && openMode === 'redirect') {
        event.preventDefault();
        openExternalModal(url);
      }
    });

    el.prevBtn.addEventListener('click', () => {
      const total = document.querySelectorAll('.games-page').length;
      if (!total) return;
      currentPage = (currentPage - 1 + total) % total;
      updatePageUi();
    });
    el.nextBtn.addEventListener('click', () => {
      const total = document.querySelectorAll('.games-page').length;
      if (!total) return;
      currentPage = (currentPage + 1) % total;
      updatePageUi();
    });

    seedDefaultsIfNeeded().finally(() => {
      onValue(settingsRef, (snap) => applySettings(snap.exists() ? snap.val() : DEFAULTS));
      onValue(gamesRef, (snap) => renderPages(snap.exists() ? snap.val() : Object.fromEntries(DEFAULT_GAMES.map((g) => [g.id, g]))));
    });
  </script>

  <div class="modal-overlay" id="externalModal" aria-hidden="true">
    <div class="modal-card" style="width:min(560px,100%);">
      <div class="modal-top">
        <h2 class="modal-title">leaving fun and games</h2>
        <button class="chip-btn" id="closeExternalModalBtn" type="button">close</button>
      </div>
      <p class="modal-copy">This game or app is hosted by an external website.</p>
      <div class="patch-notes-body" style="margin-bottom:16px;">
        <div style="font-weight:800; margin-bottom:8px;">Destination</div>
        <div id="externalHostText" style="word-break:break-word;"></div>
      </div>
      <div style="display:flex; gap:12px; flex-wrap:wrap;">
        <button class="chip-btn chatbox" id="continueExternalBtn" type="button">continue to external site</button>
        <button class="chip-btn" id="cancelExternalBtn" type="button">cancel</button>
      </div>
    </div>
  </div>

</body>
</html>