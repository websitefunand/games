<!DOCTYPE html>
<html lang="en"><head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>fun and games — geometry dash</title>
  <style>
    * { box-sizing: border-box; }

    :root {
      --bg-1: #0b1220;
      --bg-2: #111827;
      --panel: rgba(15, 23, 42, 0.72);
      --panel-strong: rgba(15, 23, 42, 0.92);
      --card-border: rgba(255, 255, 255, 0.12);
      --text: #f8fafc;
      --muted: #a5b4cc;
      --shadow: 0 20px 60px rgba(0, 0, 0, 0.34);
      --accent: #60a5fa;
      --accent-2: #22d3ee;
      --dash-gradient: linear-gradient(135deg, #38bdf8 0%, #2563eb 48%, #8b5cf6 100%);
    }

    html, body {
      margin: 0;
      min-height: 100%;
    }

    body {
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      color: var(--text);
      background:
        radial-gradient(circle at top left, rgba(56, 189, 248, 0.18), transparent 30%),
        radial-gradient(circle at top right, rgba(139, 92, 246, 0.16), transparent 28%),
        linear-gradient(160deg, var(--bg-1) 0%, var(--bg-2) 52%, #09111f 100%);
      overflow-x: hidden;
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      background-image:
        linear-gradient(rgba(255,255,255,0.035) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.035) 1px, transparent 1px);
      background-size: 32px 32px;
      mask-image: linear-gradient(to bottom, rgba(0,0,0,0.65), transparent 92%);
    }

    .page-wrap {
      width: min(1400px, calc(100% - 32px));
      margin: 0 auto;
      padding: 28px 0 42px;
      position: relative;
      z-index: 1;
    }

    .shell {
      background: var(--panel);
      border: 1px solid var(--card-border);
      border-radius: 38px;
      box-shadow: var(--shadow);
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);
      overflow: hidden;
    }

    .hero {
      position: relative;
      padding: 28px 28px 22px;
      border-bottom: 1px solid rgba(255,255,255,0.08);
      background:
        radial-gradient(circle at 20% 0%, rgba(56, 189, 248, 0.14), transparent 30%),
        radial-gradient(circle at 80% 15%, rgba(139, 92, 246, 0.12), transparent 24%),
        linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0));
    }

    .topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 26px;
      flex-wrap: wrap;
    }

    .chip-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      min-height: 54px;
      padding: 0 22px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(255,255,255,0.08);
      color: var(--text);
      text-decoration: none;
      font-weight: 700;
      letter-spacing: -0.02em;
      transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.05);
    }

    .chip-btn:hover {
      transform: translateY(-2px);
      background: rgba(255,255,255,0.12);
      border-color: rgba(255,255,255,0.25);
    }

    .hero-main {
      display: grid;
      grid-template-columns: 1.15fr 0.85fr;
      gap: 24px;
      align-items: end;
    }

    .eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 12px;
      border-radius: 999px;
      font-size: 0.82rem;
      font-weight: 700;
      color: #dbeafe;
      background: rgba(59, 130, 246, 0.16);
      border: 1px solid rgba(96, 165, 250, 0.24);
      margin-bottom: 16px;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    .site-title {
      margin: 0;
      font-size: clamp(3rem, 6vw, 5.4rem);
      line-height: 0.9;
      letter-spacing: -0.07em;
      font-weight: 900;
      text-transform: lowercase;
    }

    .site-subtitle {
      margin: 16px 0 0;
      max-width: 720px;
      font-size: 1.05rem;
      line-height: 1.65;
      color: var(--muted);
    }

    .hero-stats {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 14px;
    }

    .stat-card {
      padding: 18px;
      border-radius: 22px;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.08);
      min-height: 110px;
    }

    .stat-label {
      font-size: 0.84rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--muted);
      margin-bottom: 8px;
      font-weight: 700;
    }

    .stat-value {
      font-size: 1.9rem;
      font-weight: 800;
      letter-spacing: -0.05em;
      margin-bottom: 4px;
    }

    .stat-note {
      font-size: 0.95rem;
      color: #d8e5f7;
    }

    .content {
      padding: 28px;
    }

    .player-layout {
      display: grid;
      grid-template-columns: minmax(0, 1.45fr) minmax(320px, 0.55fr);
      gap: 24px;
      align-items: start;
    }

    .viewer-card,
    .info-card {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 30px;
      overflow: hidden;
      box-shadow: 0 18px 45px rgba(0,0,0,0.18);
    }

    .viewer-head,
    .info-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      padding: 18px 20px;
      border-bottom: 1px solid rgba(255,255,255,0.08);
      background: rgba(255,255,255,0.03);
    }

    .viewer-title,
    .info-title {
      margin: 0;
      font-size: 1.05rem;
      font-weight: 800;
      letter-spacing: -0.03em;
    }

    .viewer-meta {
      display: flex;
      align-items: center;
      gap: 10px;
      color: var(--muted);
      font-size: 0.92rem;
      font-weight: 700;
    }

    .viewer-actions {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .fullscreen-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 42px;
      padding: 0 16px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(255,255,255,0.08);
      color: var(--text);
      font: inherit;
      font-weight: 800;
      letter-spacing: -0.02em;
      cursor: pointer;
      transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.05);
    }

    .fullscreen-btn:hover {
      transform: translateY(-2px);
      background: rgba(255,255,255,0.12);
      border-color: rgba(255,255,255,0.25);
    }

    .status-dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: #22c55e;
      box-shadow: 0 0 14px rgba(34, 197, 94, 0.9);
    }

    .viewer {
      position: relative;
      background: #fff;
      aspect-ratio: 16 / 10.2;
      min-height: 520px;
    }

    .game-frame {
      width: 100%;
      height: 100%;
      border: 0;
      display: block;
      background: #fff;
    }

    .viewer-fallback {
      position: absolute;
      inset: 0;
      display: none;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 32px;
      font-weight: 800;
      font-size: 1.1rem;
      letter-spacing: -0.03em;
      background: #efefef;
      color: #0f172a;
    }

    .viewer-fallback.show {
      display: flex;
    }

    .viewer-fallback .inner {
      max-width: 620px;
    }

    .viewer-fallback a {
      color: #2563eb;
    }

    .info-body {
      padding: 20px;
    }

    .cover-card {
      background: var(--dash-gradient);
      color: #eff6ff;
      border-radius: 26px;
      padding: 20px;
      min-height: 230px;
      position: relative;
      overflow: hidden;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.25);
    }

    .cover-card::before {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(180deg, rgba(255,255,255,0.18), rgba(255,255,255,0.02) 48%, rgba(0,0,0,0.08));
      pointer-events: none;
    }

    .game-tag {
      position: relative;
      display: inline-flex;
      align-items: center;
      padding: 8px 12px;
      border-radius: 999px;
      background: rgba(255,255,255,0.18);
      font-size: 0.8rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin-bottom: 18px;
    }

    .game-title {
      position: relative;
      margin: 0;
      font-size: clamp(2rem, 4vw, 3.1rem);
      line-height: 0.92;
      font-weight: 900;
      letter-spacing: -0.06em;
      text-transform: lowercase;
      max-width: 8ch;
    }

    .game-copy {
      position: relative;
      margin: 14px 0 0;
      max-width: 28ch;
      font-size: 0.98rem;
      line-height: 1.5;
      color: rgba(239,246,255,0.88);
      font-weight: 700;
    }

    .shape-stack {
      position: absolute;
      right: 18px;
      bottom: 16px;
      display: grid;
      gap: 10px;
      transform: rotate(-8deg);
    }

    .shape {
      width: 68px;
      height: 68px;
      border-radius: 18px;
      background: rgba(255,255,255,0.22);
      border: 2px solid rgba(255,255,255,0.2);
      backdrop-filter: blur(5px);
    }

    .shape:nth-child(2) {
      width: 54px;
      height: 54px;
      margin-left: 24px;
      transform: rotate(45deg);
      border-radius: 14px;
    }

    .shape:nth-child(3) {
      width: 84px;
      height: 16px;
      margin-left: 4px;
      border-radius: 999px;
    }

    .action-group {
      display: grid;
      gap: 12px;
      margin-top: 18px;
    }

    .action-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      min-height: 56px;
      padding: 0 18px;
      border-radius: 18px;
      text-decoration: none;
      border: 1px solid rgba(255,255,255,0.12);
      font-weight: 800;
      font-size: 0.98rem;
      letter-spacing: -0.02em;
      cursor: pointer;
      transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
    }

    .action-btn:hover {
      transform: translateY(-2px);
    }

    .action-btn.primary {
      background: linear-gradient(135deg, #38bdf8, #3b82f6);
      color: #fff;
      border-color: rgba(255,255,255,0.08);
    }

    .action-btn.secondary {
      background: rgba(255,255,255,0.06);
      color: var(--text);
    }

    .details-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 12px;
      margin-top: 18px;
    }

    .detail-card {
      padding: 16px;
      border-radius: 20px;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.08);
    }

    .detail-label {
      color: var(--muted);
      font-size: 0.76rem;
      font-weight: 800;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      margin-bottom: 6px;
    }

    .detail-value {
      font-size: 1rem;
      font-weight: 800;
      letter-spacing: -0.03em;
    }

    .credit-line {
      margin-top: 18px;
      padding-top: 18px;
      border-top: 1px solid rgba(255,255,255,0.08);
      color: var(--muted);
      font-size: 0.92rem;
      line-height: 1.55;
    }

    @media (max-width: 1100px) {
      .hero-main,
      .player-layout {
        grid-template-columns: 1fr;
      }

      .viewer {
        min-height: 460px;
      }
    }

    @media (max-width: 760px) {
      .page-wrap {
        width: min(100% - 20px, 1400px);
        padding: 12px 0 24px;
      }

      .hero,
      .content {
        padding: 18px;
      }

      .topbar {
        flex-direction: column;
        align-items: stretch;
      }

      .chip-btn,
      .action-btn {
        width: 100%;
      }

      .hero-stats,
      .details-grid {
        grid-template-columns: 1fr;
      }

      .viewer {
        min-height: 360px;
      }

      .viewer-head,
      .info-head,
      .info-body {
        padding: 16px;
      }

      .game-title {
        max-width: none;
      }
    }
  </style>
</head>
<body>
  <div class="page-wrap">
    <div class="shell">
      <header class="hero">
        <div class="topbar">
          <a class="chip-btn" href="index.html">← back to homepage</a>
          
        </div>

        <div class="hero-main">
          <div>
            
            <h1 class="site-title">placeholder</h1>
            <p class="site-subtitle">description i could add while adding game</p>
          </div>

          <div class="hero-stats">
            <div class="stat-card">
              <div class="stat-label">Platform</div>
              <div class="stat-value">placeholder</div>
            </div>
            <div class="stat-card">
              <div class="stat-label">status</div>
              <div class="stat-value">placeholder</div>
            </div>
            <div class="stat-card">
              <div class="stat-label">Added</div>
              <div class="stat-value">placeholder</div>
            </div>
            <div class="stat-card">
              <div class="stat-label">Credit</div>
              <div class="stat-value">placeholder</div>
            </div>
          </div>
        </div>
      </header>

      <main class="content">
        <section class="player-layout">
          <div class="viewer-card">
            <div class="viewer-head">
              <h2 class="viewer-title">Game</h2>
              <div class="viewer-actions">
                <button id="fullscreenBtn" class="fullscreen-btn" type="button">fullscreen</button>
                <div class="viewer-meta">
                  <span class="status-dot"></span>
                  live
                </div>
              </div>
            </div>

            <div class="viewer" aria-label="Geometry Dash window">
              <iframe id="gameFrame" class="game-frame" src="placeholder" title="placeholder" loading="eager" referrerpolicy="no-referrer" allow="fullscreen *; autoplay *; clipboard-read *; clipboard-write *" allowfullscreen webkitallowfullscreen mozallowfullscreen="">
              </iframe>

              <div class="viewer-fallback" id="fallback">
                <div class="inner">
                  This game may block being shown inside an embedded frame.<br><br>
                  Use the open button to play it directly in a new tab:
                  <br><br>
                  <a href="https://geometrylitepc.io/geo-dash-lite-pc.embed" target="_blank" rel="noopener noreferrer">Open Geometry Dash</a>
                </div>
              </div>
            </div>
          </div>

          <aside class="info-card">
            <div class="info-head">
              <h2 class="info-title">Game info</h2>
            </div>

            <div class="info-body">
              <div class="cover-card">
                <h3 class="game-title">placeholder</h3>
                

                
              </div>

              <div class="action-group">
                <button class="action-btn primary" id="openGameBtn" type="button">open external source</button>
                <a class="action-btn secondary" href="index.html">go back to fun and games</a>
              </div>

              <div class="external-disclosure" id="externalDisclosure">
                This item is hosted by an <strong>external provider</strong>. Stay on trusted sites, and do not enter passwords, payment info, or personal data unless you fully trust the destination.
              </div>

              <div class="details-grid">
                <div class="detail-card">
                  <div class="detail-label">Source</div>
                  <div class="detail-value">placeholder</div>
                </div>
                
                
                <div class="detail-card">
                  <div class="detail-label">Credits</div>
                  <div class="detail-value">placeholder</div>
                </div>
              </div>

              <div class="credit-line">have fun and be safe!</div>
            </div>
          </aside>
        </section>
      </main>
    </div>
  </div>

  
<script type="module">
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
  import { getDatabase, ref, get } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-database.js";

  const firebaseConfig = {
    apiKey: "AIzaSyDvKpZacWOgca6PtoA6wP2QFX3JbI-Dvw4",
    authDomain: "funandgames-4e8c1.firebaseapp.com",
    databaseURL: "https://funandgames-4e8c1-default-rtdb.firebaseio.com",
    projectId: "funandgames-4e8c1",
    storageBucket: "funandgames-4e8c1.firebasestorage.app",
    messagingSenderId: "240713956788",
    appId: "1:240713956788:web:37b64a7a534f1e2bea06e1",
    measurementId: "G-KLSV3Q73MR"
  };

  const app = initializeApp(firebaseConfig);
  const db = getDatabase(app);

  const params = new URLSearchParams(window.location.search);
  const gameId = params.get('id');

  const openGameBtn = document.getElementById('openGameBtn');
  const frame = document.getElementById('gameFrame');
  const fallback = document.getElementById('fallback');
  const fullscreenBtn = document.getElementById('fullscreenBtn');
  const viewer = document.querySelector('.viewer');

  const pageTitle = document.querySelector('.site-title');
  const pageSubtitle = document.querySelector('.site-subtitle');
  const statValues = document.querySelectorAll('.stat-value');
  const viewerTitle = document.querySelector('.viewer-title');
  const viewerMeta = document.querySelector('.viewer-meta');
  const gameTag = document.querySelector('.cover-card .game-tag');
  const gameTitle = document.querySelector('.cover-card .game-title');
  const detailValues = document.querySelectorAll('.detail-value');
  const creditLine = document.querySelector('.credit-line');
  const fallbackLink = fallback.querySelector('a');
  if (frame) {
    frame.src = 'about:blank';
    frame.style.background = '#000';
  }

  const escapeHtml = (value='') => String(value).replace(/[&<>"']/g, (ch) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  const setText = (node, value) => { if (node) node.textContent = value ?? ''; };

  function toggleFullscreen() {
    if (!viewer) return;

    try {
      const activeElement =
        document.fullscreenElement ||
        document.webkitFullscreenElement ||
        document.mozFullScreenElement ||
        document.msFullscreenElement;

      if (activeElement) {
        if (document.exitFullscreen) document.exitFullscreen();
        else if (document.webkitExitFullscreen) document.webkitExitFullscreen();
        else if (document.mozCancelFullScreen) document.mozCancelFullScreen();
        else if (document.msExitFullscreen) document.msExitFullscreen();
        return;
      }

      if (viewer.requestFullscreen) viewer.requestFullscreen();
      else if (viewer.webkitRequestFullscreen) viewer.webkitRequestFullscreen();
      else if (viewer.mozRequestFullScreen) viewer.mozRequestFullScreen();
      else if (viewer.msRequestFullscreen) viewer.msRequestFullscreen();
    } catch (error) {
      console.error(error);
    }
  }

  function syncFullscreenButton() {
    if (!fullscreenBtn) return;
    const activeElement =
      document.fullscreenElement ||
      document.webkitFullscreenElement ||
      document.mozFullScreenElement ||
      document.msFullscreenElement;
    const active = activeElement === viewer;
    fullscreenBtn.textContent = active ? 'exit fullscreen' : 'fullscreen';
  }

  if (fullscreenBtn) {
    fullscreenBtn.addEventListener('click', toggleFullscreen);
  }
  document.addEventListener('fullscreenchange', syncFullscreenButton);
  document.addEventListener('webkitfullscreenchange', syncFullscreenButton);
  document.addEventListener('mozfullscreenchange', syncFullscreenButton);
  document.addEventListener('MSFullscreenChange', syncFullscreenButton);


  function showMissing(message) {
    setText(pageTitle, 'game not found');
    setText(pageSubtitle, message || 'This item could not be loaded.');
    setText(viewerTitle, 'Unavailable');
    setText(gameTitle, 'Missing item');
    frame.removeAttribute('src');
    frame.style.display = 'none';
    fallback.classList.add('show');
    fallback.querySelector('.inner').innerHTML = `${escapeHtml(message || 'This item could not be loaded.')}<br><br><a href="index.html">Go back to fun and games</a>`;
    if (openGameBtn) openGameBtn.style.display = 'none';
  }

  try {
    if (!gameId) {
      showMissing('No game id was provided.');
      throw new Error('missing id');
    }

    const snap = await get(ref(db, `homepageAdmin/games/${gameId}`));
    if (!snap.exists()) {
      showMissing('That game or app does not exist anymore.');
      throw new Error('game not found');
    }

    const game = snap.val() || {};
    const launchUrl = game.sourceUrl || game.embedUrl || '';

    if (game.locked) {
      window.location.href = 'construction.html';
      throw new Error('locked');
    }

    if (game.openMode === 'redirect') {
      window.location.href = launchUrl;
      throw new Error('redirected');
    }

    document.title = `${game.title || 'fun and games'} - fun and games`;
    setText(pageTitle, game.title || 'untitled');
    setText(pageSubtitle, game.description || 'launching your selected item');
    if (statValues[0]) statValues[0].textContent = game.platform || 'browser';
    if (statValues[1]) statValues[1].textContent = game.statusText || 'live';
    if (statValues[2]) statValues[2].textContent = game.addedText || 'new';
    if (statValues[3]) statValues[3].textContent = game.credits || 'original creators';
    setText(viewerTitle, game.type === 'app' ? 'App' : 'Game');
    if (viewerMeta) viewerMeta.innerHTML = '<span class="status-dot"></span>' + escapeHtml(game.statusText || 'live');
    setText(gameTag, game.tag || game.type || 'game');
    setText(gameTitle, game.title || 'untitled');
    if (detailValues[0]) detailValues[0].textContent = game.sourceLabel || 'launch url';
    if (detailValues[1]) detailValues[1].textContent = game.credits || 'original creators';
    setText(creditLine, game.description || 'have fun and be safe!');

    frame.src = game.embedUrl || launchUrl;
    frame.title = game.title || 'fun and games item';
    if (fallbackLink) {
      fallbackLink.href = launchUrl || '#';
      fallbackLink.textContent = `Open ${game.title || 'item'}`;
    }
    if (openGameBtn) {
      openGameBtn.addEventListener('click', () => {
        window.open(launchUrl || frame.src, '_blank', 'noopener,noreferrer');
      });
    }

    let frameLoaded = false;
    frame.addEventListener('load', () => { frameLoaded = true; });
    setTimeout(() => {
      if (!frameLoaded) {
        fallback.classList.add('show');
      }
    }, 1200);
  } catch (error) {
    console.error(error);
  }
</script>

</body></html>
